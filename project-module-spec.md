# 📦 项目模块化规范

> 版本：v1.0
> 适用：所有 Orchestra 管理的项目
> 目的：让项目代码像乐高积木一样可拆可装

---

## 一、核心理念

### 1.1 为什么要模块化

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│  没有模块化                          有模块化                               │
│  ────────────                        ────────────                           │
│                                                                             │
│  "订单页面有 bug"                    "订单页面有 bug"                       │
│       ↓                                   ↓                                 │
│  在 10000 行代码里找                 查 modules.yaml                        │
│       ↓                                   ↓                                 │
│  找了 2 小时                         page-orders → src/pages/orders/        │
│       ↓                                   ↓                                 │
│  改了一处，另一处坏了                5 分钟定位，改动只影响该模块            │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 模块化三原则

| 原则 | 含义 | 检验方法 |
|------|------|---------|
| **单一职责** | 一个模块只做一件事 | 能否一句话说清这模块干嘛 |
| **清晰边界** | 模块间通过明确接口通信 | 改模块内部不影响外部 |
| **可追溯** | 任何功能都能找到对应模块 | modules.yaml 有记录 |

---

## 二、目录结构规范

### 2.1 前端项目（React/Vue/通用）

```
project-root/
│
├── modules.yaml                    # 📋 模块注册清单（必须）
├── README.md                       # 项目说明
│
├── src/
│   │
│   ├── pages/                      # 📄 页面模块
│   │   ├── home/                   # 每个页面一个目录
│   │   │   ├── index.tsx           # 页面入口
│   │   │   ├── components/         # 页面私有组件
│   │   │   ├── hooks/              # 页面私有 hooks
│   │   │   ├── styles.css          # 页面样式
│   │   │   └── README.md           # 页面说明（可选）
│   │   │
│   │   ├── orders/
│   │   │   ├── index.tsx
│   │   │   ├── OrderList.tsx       # 子组件
│   │   │   ├── OrderFilter.tsx
│   │   │   └── useOrders.ts        # 页面专用 hook
│   │   │
│   │   └── ...
│   │
│   ├── components/                 # 🧩 公共组件模块
│   │   ├── Button/
│   │   │   ├── index.tsx           # 组件入口
│   │   │   ├── Button.tsx          # 组件实现
│   │   │   ├── Button.test.tsx     # 组件测试
│   │   │   ├── styles.css
│   │   │   └── README.md           # 组件文档
│   │   │
│   │   ├── Table/
│   │   ├── Modal/
│   │   └── ...
│   │
│   ├── services/                   # 🔌 服务模块（API 调用）
│   │   ├── auth/
│   │   │   ├── index.ts            # 服务入口
│   │   │   ├── api.ts              # API 定义
│   │   │   ├── types.ts            # 类型定义
│   │   │   └── README.md
│   │   │
│   │   ├── order/
│   │   ├── user/
│   │   └── ...
│   │
│   ├── hooks/                      # 🎣 公共 Hooks 模块
│   │   ├── useAuth/
│   │   ├── usePagination/
│   │   └── ...
│   │
│   ├── stores/                     # 📦 状态管理模块
│   │   ├── auth/
│   │   ├── cart/
│   │   └── ...
│   │
│   ├── utils/                      # 🔧 工具模块
│   │   ├── format/
│   │   ├── validation/
│   │   └── ...
│   │
│   └── types/                      # 📝 全局类型定义
│       ├── api.ts
│       ├── models.ts
│       └── ...
│
├── public/                         # 静态资源
└── tests/                          # 集成测试
```

### 2.2 后端项目（Node/Python/通用）

```
project-root/
│
├── modules.yaml                    # 📋 模块注册清单（必须）
├── README.md
│
├── src/
│   │
│   ├── api/                        # 🌐 API 路由模块
│   │   ├── auth/
│   │   │   ├── index.ts            # 路由入口
│   │   │   ├── controller.ts       # 控制器
│   │   │   ├── validator.ts        # 参数校验
│   │   │   └── README.md
│   │   │
│   │   ├── orders/
│   │   ├── users/
│   │   └── ...
│   │
│   ├── services/                   # 💼 业务逻辑模块
│   │   ├── auth/
│   │   │   ├── index.ts
│   │   │   ├── AuthService.ts
│   │   │   └── README.md
│   │   │
│   │   ├── order/
│   │   ├── payment/
│   │   └── ...
│   │
│   ├── models/                     # 📊 数据模型模块
│   │   ├── User.ts
│   │   ├── Order.ts
│   │   └── ...
│   │
│   ├── repositories/               # 🗄️ 数据访问模块
│   │   ├── UserRepository.ts
│   │   ├── OrderRepository.ts
│   │   └── ...
│   │
│   ├── middlewares/                # 🔗 中间件模块
│   │   ├── auth.ts
│   │   ├── errorHandler.ts
│   │   └── ...
│   │
│   ├── utils/                      # 🔧 工具模块
│   │   ├── logger/
│   │   ├── crypto/
│   │   └── ...
│   │
│   └── types/                      # 📝 类型定义
│
├── config/                         # 配置文件
├── scripts/                        # 脚本
└── tests/                          # 测试
```

### 2.3 全栈项目（Monorepo）

```
project-root/
│
├── modules.yaml                    # 📋 全局模块清单
├── README.md
│
├── packages/
│   │
│   ├── web/                        # 前端
│   │   ├── modules.yaml            # 前端模块清单
│   │   └── src/...
│   │
│   ├── api/                        # 后端
│   │   ├── modules.yaml            # 后端模块清单
│   │   └── src/...
│   │
│   └── shared/                     # 共享代码
│       ├── modules.yaml
│       ├── types/
│       └── utils/
│
└── tools/                          # 工具脚本
```

---

## 三、模块结构规范

### 3.1 标准模块结构

每个模块（无论类型）都应包含：

```
module-name/
├── index.ts          # 【必须】入口文件，统一导出
├── README.md         # 【推荐】模块说明
├── *.test.ts         # 【推荐】测试文件
└── ...               # 具体实现文件
```

### 3.2 入口文件规范

```typescript
// index.ts - 统一导出，外部只通过此文件访问模块

// 导出主要功能
export { OrderService } from './OrderService';
export { createOrder, getOrderList } from './api';

// 导出类型
export type { Order, OrderStatus, CreateOrderParams } from './types';

// 默认导出（如果有）
export { OrderService as default } from './OrderService';
```

### 3.3 模块 README 规范

```markdown
# 模块名称

> 一句话说明模块职责

## 职责

- 这个模块负责什么
- 不负责什么（明确边界）

## 接口

### 导出的函数/类/组件

| 名称 | 类型 | 说明 |
|------|------|------|
| OrderService | class | 订单业务逻辑 |
| createOrder | function | 创建订单 |

### Props（如果是组件）

| 属性 | 类型 | 必填 | 说明 |
|------|------|------|------|
| data | Order[] | 是 | 订单数据 |

## 依赖

- `services/auth` - 用户认证
- `components/Table` - 表格展示

## 使用示例

```typescript
import { OrderService } from '@/services/order';

const orderService = new OrderService();
const orders = await orderService.getList();
```

## 版本历史

| 日期 | 版本 | 变更 |
|------|------|------|
| 2024-01-20 | 1.0 | 初始版本 |
```

---

## 四、模块类型定义

### 4.1 模块类型清单

| 类型 | 标识 | 说明 | 典型内容 |
|------|------|------|---------|
| 页面 | `page` | 独立路由页面 | 页面组件、页面逻辑 |
| 组件 | `component` | 可复用 UI 组件 | React/Vue 组件 |
| 服务 | `service` | 业务逻辑/API | API 调用、业务处理 |
| Hook | `hook` | 可复用逻辑 | React Hooks |
| 状态 | `store` | 全局状态 | Redux/Zustand/Pinia |
| 工具 | `util` | 通用工具函数 | 格式化、校验 |
| 模型 | `model` | 数据模型 | 数据库模型、类型 |
| 中间件 | `middleware` | 请求处理 | 认证、日志 |
| 配置 | `config` | 配置项 | 环境变量、常量 |

### 4.2 模块状态定义

| 状态 | 说明 | 可否修改 |
|------|------|---------|
| `stable` | 稳定，已测试 | 慎重修改 |
| `dev` | 开发中 | 可自由修改 |
| `deprecated` | 废弃，待移除 | 不要新增依赖 |
| `experimental` | 实验性 | 可能大改 |

---

## 五、模块依赖规则

### 5.1 依赖方向（单向）

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│  合法依赖方向：从上到下                                                     │
│                                                                             │
│  pages           ← 可依赖 components, services, hooks, stores, utils       │
│     ↓                                                                       │
│  components      ← 可依赖 hooks, utils                                      │
│     ↓                                                                       │
│  services        ← 可依赖 models, utils, types                              │
│     ↓                                                                       │
│  hooks           ← 可依赖 services, utils                                   │
│     ↓                                                                       │
│  stores          ← 可依赖 services, types                                   │
│     ↓                                                                       │
│  utils           ← 只依赖 types 或无依赖                                    │
│     ↓                                                                       │
│  types           ← 无依赖                                                   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.2 禁止的依赖

```yaml
forbidden_dependencies:

  # 循环依赖
  circular:
    bad: "A → B → A"
    error: "模块循环依赖，必须重构"
    
  # 反向依赖
  reverse:
    bad: "utils → pages"
    error: "底层模块不能依赖上层"
    
  # 跨层依赖
  cross_layer:
    bad: "component A → component B 的内部文件"
    error: "只能依赖模块入口，不能依赖内部实现"
```

### 5.3 依赖检查

```yaml
dependency_check:

  rule_1: "只通过 index.ts 导入"
    good: "import { Button } from '@/components/Button'"
    bad: "import { Button } from '@/components/Button/Button.tsx'"
    
  rule_2: "显式声明依赖"
    方法: "每个模块的 README 列出依赖"
    验证: "modules.yaml 记录依赖关系"
    
  rule_3: "依赖要精简"
    原则: "只依赖真正需要的"
    检查: "每个依赖都有使用吗？"
```

---

## 六、命名规范

### 6.1 目录/文件命名

| 类型 | 风格 | 示例 |
|------|------|------|
| 目录 | kebab-case | `order-detail/`, `user-profile/` |
| 组件文件 | PascalCase | `OrderList.tsx`, `UserCard.vue` |
| 其他文件 | camelCase | `useAuth.ts`, `formatDate.ts` |
| 测试文件 | 同名 + .test | `OrderList.test.tsx` |
| 样式文件 | 同名 + .css | `OrderList.css` |

### 6.2 模块 ID 命名

```yaml
module_id_format: "{type}-{name}"

examples:
  - "page-home"
  - "page-order-list"
  - "page-order-detail"
  - "component-button"
  - "component-data-table"
  - "service-auth"
  - "service-order"
  - "hook-use-pagination"
  - "store-cart"
  - "util-format"
```

---

## 七、模块创建检查清单

创建新模块时，检查以下项目：

```yaml
checklist:

  # 结构
  - [ ] 有 index.ts 统一导出
  - [ ] 有 README.md 说明（推荐）
  - [ ] 有测试文件（推荐）
  
  # 命名
  - [ ] 目录名符合 kebab-case
  - [ ] 模块 ID 符合 {type}-{name}
  
  # 依赖
  - [ ] 依赖方向正确（上→下）
  - [ ] 无循环依赖
  - [ ] 只通过 index 导入其他模块
  
  # 注册
  - [ ] 已添加到 modules.yaml
  - [ ] dependencies 已声明
  
  # 文档
  - [ ] 职责清晰（一句话能说清）
  - [ ] 接口已文档化
```

---

## 八、问题定位流程

### 8.1 标准定位流程

```
问题："XX 功能有 bug"
     ↓
Step 1: 查 modules.yaml
     → 找到功能对应的模块 ID 和路径
     ↓
Step 2: 查模块 README
     → 了解模块职责和依赖
     ↓
Step 3: 判断问题在哪
     → 本模块？还是依赖的模块？
     ↓
Step 4: 定位具体文件
     → 根据模块结构找到相关代码
     ↓
Step 5: 修复
     → 只改必要的文件
     ↓
Step 6: 验证
     → 运行模块测试，确保不影响其他
```

### 8.2 快速定位命令

```bash
# 查找模块
grep "功能关键词" modules.yaml

# 查看模块依赖
grep -A 5 "id: page-orders" modules.yaml

# 查看谁依赖这个模块
grep "service-order" modules.yaml
```

---

## 九、版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2024-01-20 | 初始版本 |

---

**📦 项目模块化规范 · 完**
