---
name: verification-executor
description: |
  验证执行官（督工御史）- 独立执行所有验证命令，确保验证结果真实可信。
  核心职责：防止纸上谈兵，确保 Test Agent 的所有验证都是真实独立执行的。
  专属服务 Test Agent，是 Test Agent 独立执行原则的执行者。
  Use when (1) 执行编译验证, (2) 执行测试验证, (3) 验证 Skills 协作状态, (4) 生成验证证据链, (5) 服务启动验证。
---

# 🔍 验证执行官（督工御史）

> Orchestra 体系 · Test Agent 专属核心 Skill
> 版本：v1.1
> 核心原则：独立执行，眼见为实，禁止复用
> 铁律总数：10 条

---

## 📌 目录

1. [一、核心架构](#一核心架构)
2. [二、核心定位](#二核心定位)
3. [三、验证前准备](#三验证前准备)
4. [四、独立执行清单（核心）](#四独立执行清单核心)
5. [五、Skills 协作状态验证（关键新增）](#五skills-协作状态验证关键新增)
6. [六、证据链生成规范](#六证据链生成规范)
7. [七、铁律清单](#七铁律清单)
8. [八、调用接口](#八调用接口)
9. [九、与 Test Agent 交接](#九与-test-agent-交接)
10. [十、版本历史](#十版本历史)

---

## 一、核心架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│  ⚠️ 【铁律】验证执行唯一入口                                                │
│  ═══════════════════════════════════════════════════════════════════════   │
│                                                                             │
│  Test Agent 执行任何验证时，                                                │
│  必须且只能通过「验证执行官」（本 Skill）执行验证命令。                     │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                                                                     │   │
│  │   Code Agent 交付代码                                               │   │
│  │          │                                                          │   │
│  │          ▼                                                          │   │
│  │   Test Agent ──→ 🔍 验证执行官 ──→ 验证结果                         │   │
│  │          │              │                                           │   │
│  │          │              ├─→ 独立执行命令                            │   │
│  │          │              ├─→ 记录完整输出                            │   │
│  │          │              ├─→ 生成证据链                              │   │
│  │          │              └─→ 验证 Skills 状态                        │   │
│  │          │                                                          │   │
│  │          ▼                                                          │   │
│  │   验收判定（基于真实证据）                                          │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  【核心原则】                                                               │
│  1. 独立执行 - 自己执行命令，不复用 Code Agent 输出                        │
│  2. 眼见为实 - 只相信自己执行后的真实输出                                  │
│  3. 证据完整 - 命令→输出→判定，缺一不可                                   │
│  4. 状态验证 - 验证 Skills 协作状态文件                                    │
│                                                                             │
│  【禁止行为】                                                               │
│  ❌ 复用 Code Agent 说的"编译通过"                                        │
│  ❌ 复用 Code Agent 说的"测试通过"                                        │
│  ❌ 只写结论不贴输出                                                       │
│  ❌ 截断输出隐藏错误                                                       │
│  ❌ 跳过 Skills 状态文件检查                                               │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 二、核心定位

```yaml
role:
  name: "验证执行官"
  alias: "督工御史"

  metaphor: |
    如同督工御史巡查工程：
    - 不听工匠说"已完工"，必须亲眼验查
    - 逐项检验，留取证据
    - 发现问题立即上报，不得隐瞒

  position_in_orchestra: |
    Test Agent ──→ 【验证执行官】──→ 验证结果
         │               │
         │               └─→ 独立执行，生成证据
         │
         ▼
    验收判定（基于真实证据）

  core_responsibility: |
    确保 Test Agent 的所有验证都是真实独立执行的，
    防止"纸上谈兵"，防止复用 Code Agent 的自检结果，
    生成可追溯、可验证的证据链。
```

---

## 三、验证前准备

> 每次验收必须执行，确保验证环境干净

```yaml
pre_verification:

  # ========== 环境确认 ==========
  step_1_环境确认:
    命令: |
      echo "=== 验证环境 ==="
      echo "工作目录: $(pwd)"
      echo "Node.js: $(node -v)"
      echo "npm: $(npm -v)"
      echo "时间: $(date '+%Y-%m-%d %H:%M:%S')"
    目的: "记录验证环境，确保可重复"
    输出格式: |
      === 验证环境 ===
      工作目录: /path/to/project
      Node.js: v20.x.x
      npm: 10.x.x
      时间: 2026-02-02 10:00:00

  # ========== 依赖安装 ==========
  step_2_依赖安装:
    命令: "npm ci"
    目的: "确保依赖与 lockfile 一致"
    说明: "不使用 npm install，避免依赖版本漂移"
    失败处理: "记录错误，上报 Test Agent"

  # ========== 清理缓存 ==========
  step_3_清理缓存:
    命令: |
      rm -rf dist/
      rm -rf coverage/
      rm -rf .turbo/
      rm -rf node_modules/.cache/
    目的: "确保是干净的构建，不受缓存影响"

  # ========== 数据库准备（如需）==========
  step_4_数据库准备:
    条件: "项目使用数据库"
    命令: |
      npx prisma migrate deploy
      # 或
      npx prisma db push
    目的: "确保数据库 schema 同步"
```

---

## 四、独立执行清单（核心）

> 每个验证项必须独立执行，禁止复用 Code Agent 输出

```yaml
independent_execution_checklist:

  # ═══════════════════════════════════════════════════════════════════
  # 2.1 编译验证
  # ═══════════════════════════════════════════════════════════════════
  VE-01_编译验证:
    Code_Agent_可能说: "编译通过"
    督工御史必须做:
      命令: "npx tsc --noEmit"
      记录: "完整输出（包括警告）"
    判定依据: |
      - 无输出 = 通过
      - 有 error = 失败
      - 只有 warning = 条件通过
    证据格式: |
      ### VE-01 编译验证

      **执行时间**：{timestamp}

      **执行命令**：
      ```bash
      npx tsc --noEmit
      ```

      **完整输出**：
      ```
      {output}
      ```

      **判定结果**：{PASS/FAIL}
      **判定依据**：{reason}

  # ═══════════════════════════════════════════════════════════════════
  # 2.2 Lint 验证
  # ═══════════════════════════════════════════════════════════════════
  VE-02_Lint验证:
    Code_Agent_可能说: "Lint 通过"
    督工御史必须做:
      命令: "npx eslint . --ext .ts,.tsx,.js,.jsx"
      记录: "完整输出"
    判定依据: |
      - 0 errors = 通过
      - 有 errors = 失败
      - 只有 warnings = 条件通过
    证据格式: 同 VE-01

  # ═══════════════════════════════════════════════════════════════════
  # 2.3 单元测试
  # ═══════════════════════════════════════════════════════════════════
  VE-03_单元测试:
    Code_Agent_可能说: "测试通过，覆盖率 85%"
    督工御史必须做:
      命令: "npm run test -- --coverage"
      记录: |
        - 完整测试输出
        - 覆盖率报告
    判定依据: |
      - 通过率 ≥ 80% = 通过
      - 通过率 60-80% = 条件通过
      - 通过率 < 60% = 失败
    证据格式: |
      ### VE-03 单元测试

      **执行时间**：{timestamp}

      **执行命令**：
      ```bash
      npm run test -- --coverage
      ```

      **测试结果**：
      ```
      Tests:       {passed} passed, {failed} failed, {total} total
      Time:        {time}
      ```

      **覆盖率**：
      | 指标 | 百分比 |
      |------|--------|
      | Statements | {stmts}% |
      | Branches | {branch}% |
      | Functions | {funcs}% |
      | Lines | {lines}% |

      **判定结果**：{PASS/CONDITIONAL_PASS/FAIL}
      **判定依据**：通过率 {rate}%

  # ═══════════════════════════════════════════════════════════════════
  # 2.4 服务启动验证
  # ═══════════════════════════════════════════════════════════════════
  VE-04_服务启动验证:
    Code_Agent_可能说: "服务正常启动"
    督工御史必须做:
      命令: |
        # 后台启动服务
        npm run dev &
        DEV_PID=$!

        # 等待启动
        sleep 10

        # 健康检查
        HEALTH_RESPONSE=$(curl -s http://localhost:3000/health)
        HEALTH_CODE=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/health)

        echo "Health Check Response: $HEALTH_RESPONSE"
        echo "HTTP Status Code: $HEALTH_CODE"

        # 清理
        kill $DEV_PID 2>/dev/null
      记录: |
        - HTTP 状态码
        - 响应内容
    判定依据: |
      - HTTP 200 + 响应正确 = 通过
      - HTTP 非 200 = 失败
      - 无法连接 = 失败
    证据格式: 同上

  # ═══════════════════════════════════════════════════════════════════
  # 2.5 集成测试
  # ═══════════════════════════════════════════════════════════════════
  VE-05_集成测试:
    Code_Agent_可能说: "E2E 测试通过"
    督工御史必须做:
      命令: "npm run test:e2e"
      记录: "完整输出"
    判定依据: |
      - 0 failed = 通过
      - 有 failed = 失败
    证据格式: 同上

  # ═══════════════════════════════════════════════════════════════════
  # 2.6 构建验证
  # ═══════════════════════════════════════════════════════════════════
  VE-06_构建验证:
    Code_Agent_可能说: "构建成功"
    督工御史必须做:
      命令: "npm run build"
      记录: |
        - 完整输出
        - 构建产物目录
    判定依据: |
      - 无错误 + 产物存在 = 通过
      - 有错误 = 失败
    验证产物:
      命令: "ls -la dist/"
      记录: "产物列表"
```

---

## 五、Skills 协作状态验证（关键新增）

> 验证 Code Agent 各 Skills 之间的协作是否正确完成

```yaml
skills_collaboration_verification:

  # ═══════════════════════════════════════════════════════════════════
  # 3.1 Shared 状态文件验证
  # ═══════════════════════════════════════════════════════════════════
  VS-01_Shared状态验证:
    目的: "验证 shared-coder 是否正确完成并生成状态文件"
    检查项:
      文件存在:
        命令: "ls packages/shared/SHARED_STATUS.md"
        期望: "文件存在"
      状态正确:
        命令: "grep '状态：READY' packages/shared/SHARED_STATUS.md"
        期望: "匹配到 READY 状态"
      内容完整:
        命令: "cat packages/shared/SHARED_STATUS.md"
        检查:
          - "包含完成时间"
          - "包含导出类型列表"
          - "包含验证命令"
    证据格式: |
      ### VS-01 Shared 状态验证

      **执行时间**：{timestamp}

      **1. 文件存在检查**：
      ```bash
      ls packages/shared/SHARED_STATUS.md
      ```
      输出：{output}
      结果：{PASS/FAIL}

      **2. 状态检查**：
      ```bash
      grep '状态：READY' packages/shared/SHARED_STATUS.md
      ```
      输出：{output}
      结果：{PASS/FAIL}

      **3. 完整内容**：
      ```markdown
      {file_content}
      ```

      **判定结果**：{PASS/FAIL}

  # ═══════════════════════════════════════════════════════════════════
  # 3.2 Backend 状态文件验证
  # ═══════════════════════════════════════════════════════════════════
  VS-02_Backend状态验证:
    目的: "验证 backend-coder 是否正确完成并生成状态文件"
    前提检查:
      命令: "ls packages/shared/SHARED_STATUS.md && grep -q '状态：READY' packages/shared/SHARED_STATUS.md"
      目的: "确认 backend-coder 的前置依赖（shared）已完成"
    检查项:
      文件存在:
        命令: "ls packages/backend/BACKEND_STATUS.md"
        期望: "文件存在"
      状态正确:
        命令: "grep '状态：READY' packages/backend/BACKEND_STATUS.md"
        期望: "匹配到 READY 状态"
      API变更记录:
        命令: "ls packages/backend/API_CHANGELOG.md 2>/dev/null || echo 'NO_CHANGELOG'"
        说明: "如果有 API 变更，应该有 CHANGELOG"
    证据格式: 同 VS-01

  # ═══════════════════════════════════════════════════════════════════
  # 3.3 前端 Skills 依赖验证
  # ═══════════════════════════════════════════════════════════════════
  VS-03_前端依赖验证:
    目的: "验证前端 Skills（web/mobile/desktop）是否正确依赖上游"
    适用: "当有 web/mobile/desktop 包时"
    检查项:
      web_coder检查:
        条件: "存在 packages/web"
        检查:
          - "是否正确引用 shared 类型"
          - "是否在 SHARED_STATUS 为 READY 后开始"
        命令: |
          # 检查是否引用了 shared
          grep -r "from.*shared" packages/web/src/ | head -5
      mobile_coder检查:
        条件: "存在 packages/mobile"
        检查: 同上
      desktop_coder检查:
        条件: "存在 packages/desktop"
        检查: 同上
    证据格式: |
      ### VS-03 前端依赖验证

      **检查 packages/web 对 shared 的引用**：
      ```bash
      grep -r "from.*shared" packages/web/src/ | head -5
      ```
      输出：
      ```
      {output}
      ```

      **判定**：{引用存在/引用缺失}

  # ═══════════════════════════════════════════════════════════════════
  # 3.4 API 变更通知验证
  # ═══════════════════════════════════════════════════════════════════
  VS-04_API变更通知验证:
    目的: "验证 backend-coder 的 API 变更是否正确通知下游"
    触发条件: "backend-coder 有 API 变更"
    检查项:
      CHANGELOG存在:
        命令: "ls packages/backend/API_CHANGELOG.md"
        期望: "文件存在"
      内容完整:
        命令: "cat packages/backend/API_CHANGELOG.md"
        检查:
          - "包含变更类型（新增/修改/删除）"
          - "包含受影响端点"
          - "包含下游影响说明"
      下游处理:
        说明: "检查前端是否处理了 API 变更"
        检查: "前端代码是否更新了相关 API 调用"
    证据格式: |
      ### VS-04 API 变更通知验证

      **1. CHANGELOG 存在检查**：
      ```bash
      ls packages/backend/API_CHANGELOG.md
      ```
      结果：{PASS/FAIL/NOT_APPLICABLE}

      **2. CHANGELOG 内容**：
      ```markdown
      {file_content}
      ```

      **3. 下游处理检查**：
      {检查结果}

      **判定结果**：{PASS/FAIL/NOT_APPLICABLE}

  # ═══════════════════════════════════════════════════════════════════
  # 3.5 Skills 协作完整性汇总
  # ═══════════════════════════════════════════════════════════════════
  VS-05_协作完整性汇总:
    目的: "汇总所有 Skills 协作状态，判断协作是否完整"
    汇总格式: |
      ## Skills 协作状态汇总

      | Skill | 状态文件 | 状态 | 依赖检查 |
      |-------|----------|------|----------|
      | shared-coder | SHARED_STATUS.md | {状态} | N/A |
      | backend-coder | BACKEND_STATUS.md | {状态} | shared ✅/❌ |
      | web-coder | - | - | shared ✅/❌, backend ✅/❌ |
      | mobile-coder | - | - | shared ✅/❌, backend ✅/❌ |
      | desktop-coder | - | - | shared ✅/❌, backend ✅/❌ |

      **协作完整性判定**：{PASS/FAIL}
      **问题项**：{如有}
```

---

## 六、证据链生成规范

> 每个验证必须生成完整证据链

```yaml
evidence_chain:

  # ========== 证据链三要素 ==========
  three_elements:
    命令: "执行了什么命令"
    输出: "完整的原始输出"
    判定: "根据输出做出的判断及依据"

  # ========== 标准证据格式 ==========
  standard_format: |
    ╔══════════════════════════════════════════════════════════════════╗
    ║ 验证项：{verification_name}                                       ║
    ╠══════════════════════════════════════════════════════════════════╣
    ║                                                                  ║
    ║ 执行命令：                                                        ║
    ║ $ {command}                                                      ║
    ║                                                                  ║
    ║ 执行时间：{timestamp}                                            ║
    ║                                                                  ║
    ║ 完整输出：                                                        ║
    ║ {output}                                                         ║
    ║                                                                  ║
    ║ 判定结果：{PASS/CONDITIONAL_PASS/FAIL}                           ║
    ║ 判定依据：{reason}                                               ║
    ╚══════════════════════════════════════════════════════════════════╝

  # ========== 禁止行为 ==========
  prohibited:
    - "只写结论不贴输出"
    - "只贴输出不写判定依据"
    - "截断输出隐藏错误"
    - "复制 Code Agent 的输出当作自己的"
    - "伪造时间戳"

  # ========== 可疑迹象（红旗）==========
  red_flags:
    - "输出与 Code Agent 一模一样 → 可能复用"
    - "没有时间戳 → 可能伪造"
    - "输出格式异常整齐 → 可能编造"
    - "验证时间不合理（太快）→ 可能跳过"
```

---

## 七、铁律清单

```yaml
laws:

  # ========== 独立执行铁律 ==========
  VX-01:
    name: "独立执行原则"
    rule: "所有验证命令必须由督工御史独立执行，禁止复用 Code Agent 输出"
    severity: "🔴 最高级违规"
    检测方法: "验收报告审查 - 检查时间戳和输出差异"

  VX-02:
    name: "眼见为实"
    rule: "只相信自己执行后的真实输出，不相信任何人的口头声明"
    severity: "🔴 最高级违规"
    检测方法: "证据链完整性检查"

  VX-03:
    name: "证据链完整"
    rule: "每个验证结论必须有完整证据链：命令→输出→判定"
    severity: "🔴 最高级违规"
    检测方法: "证据链三要素检查"

  VX-04:
    name: "输出不可截断"
    rule: "验证输出必须完整记录，禁止截断或隐藏"
    severity: "⚠️ 重要"
    检测方法: "输出完整性检查"

  # ========== Skills 协作验证铁律 ==========
  VX-05:
    name: "状态文件必验"
    rule: "必须验证 SHARED_STATUS.md 和 BACKEND_STATUS.md 存在且状态为 READY"
    severity: "🔴 最高级违规"
    适用: "有 monorepo 结构的项目"

  VX-06:
    name: "协作依赖必检"
    rule: "必须验证下游 Skills 正确依赖上游输出"
    severity: "⚠️ 重要"
    检测方法: "VS-03 依赖验证"

  VX-07:
    name: "API变更必查"
    rule: "如有 API 变更，必须验证 API_CHANGELOG.md 存在且下游已处理"
    severity: "⚠️ 重要"
    检测方法: "VS-04 变更通知验证"

  # ========== 环境与时间铁律 ==========
  VX-08:
    name: "环境必记录"
    rule: "每次验证必须记录验证环境（目录、版本、时间）"
    severity: "⚠️ 重要"
    检测方法: "环境信息存在性检查"

  VX-09:
    name: "依赖必同步"
    rule: "验证前必须执行 npm ci 确保依赖一致"
    severity: "⚠️ 重要"
    检测方法: "验证日志检查"

  VX-10:
    name: "缓存必清理"
    rule: "验证前必须清理构建缓存确保干净验证"
    severity: "⚠️ 重要"
    检测方法: "验证日志检查"

  # ========== 铁律汇总 ==========
  summary:
    total: 10
    严重级别分布:
      最高级违规: 4  # VX-01, VX-02, VX-03, VX-05
      重要: 6        # VX-04, VX-06, VX-07, VX-08, VX-09, VX-10
```

---

## 八、调用接口

```yaml
api:

  # ========== 完整验证流程 ==========
  execute_full_verification:
    description: "执行完整验证流程"
    input:
      project_path: "项目路径"
      verification_scope: "full | contract | implementation"
    output:
      verification_report: "完整验证报告"
      evidence_chain: "证据链文档"
    steps:
      1: "执行验证前准备"
      2: "执行独立执行清单"
      3: "执行 Skills 协作验证"
      4: "生成证据链"
      5: "生成验证报告"

  # ========== 单项验证 ==========
  execute_single_verification:
    description: "执行单项验证"
    input:
      verification_id: "VE-01 | VE-02 | ... | VS-05"
    output:
      result: "PASS | CONDITIONAL_PASS | FAIL"
      evidence: "证据链"

  # ========== Skills 状态验证 ==========
  verify_skills_collaboration:
    description: "验证 Skills 协作状态"
    input:
      project_structure: "monorepo | single-package"
    output:
      collaboration_status: "完整 | 不完整"
      issues: "问题列表"

  # ═══════════════════════════════════════════════════════════════
  # 便捷接口（Test Agent 常用调用）🆕 v1.1
  # ═══════════════════════════════════════════════════════════════

  # ========== 便捷验证接口 ==========
  execute_verification:
    description: "便捷验证接口，按类型执行单项验证"
    alias_for: "execute_single_verification 的便捷封装"
    input:
      type: "compile | lint | unit_test | server_start | integration | build"
    output:
      result: "PASS | CONDITIONAL_PASS | FAIL"
      output: "完整命令输出"
      evidence: "证据链"
    mapping:
      compile: "VE-01"
      lint: "VE-02"
      unit_test: "VE-03"
      server_start: "VE-04"
      integration: "VE-05"
      build: "VE-06"

  # ========== 层级状态验证 ==========
  verify_skills_status:
    description: "验证指定层的 Skills 协作状态"
    input:
      layer: "shared | backend | web | mobile | desktop"
    output:
      exists: boolean
      status: "READY | NOT_READY | MISSING"
      status_file: "状态文件路径"
    检查项:
      shared: "SHARED_STATUS.md 存在且状态为 READY"
      backend: "BACKEND_STATUS.md 存在且状态为 READY"
      web: "WEB_STATUS.md 存在且状态为 READY"
      mobile: "MOBILE_STATUS.md 存在且状态为 READY"
      desktop: "DESKTOP_STATUS.md 存在且状态为 READY"

  # ========== 前端依赖验证 ==========
  verify_frontend_dependencies:
    description: "验证前端正确引用 shared 和调用 backend API"
    input:
      frontend_packages: "['web', 'mobile', 'desktop'] 的子集"
    output:
      dependencies_correct: boolean
      issues: "问题列表"
    检查项:
      - "package.json 正确引用 @project/shared"
      - "import 路径正确"
      - "API 调用使用正确的类型定义"

  # ========== API 契约验证 ==========
  verify_api_contract:
    description: "验证 API 实现与 Spec 定义一致"
    input:
      spec_path: "Tech Spec 路径"
      api_path: "API 路由文件路径"
    output:
      api_contract_valid: boolean
      mismatches: "不匹配列表"
    检查项:
      - "路由路径与 Spec 一致"
      - "HTTP 方法与 Spec 一致"
      - "请求/响应类型与契约一致"

  # ========== 协作完整性汇总 ==========
  get_collaboration_summary:
    description: "获取 Skills 协作状态完整汇总"
    input: null
    output:
      all_ready: boolean
      status_table: |
        | Layer | Status | File | Issues |
        |-------|--------|------|--------|
        | shared | READY/NOT_READY | path | ... |
        | backend | READY/NOT_READY | path | ... |
        | ... | ... | ... | ... |
      issues: "所有问题汇总"
      recommendation: "下一步建议"
```

---

## 九、与 Test Agent 交接

```yaml
handover:

  # ========== 从 Test Agent 接收 ==========
  receive_from_test_agent:
    触发: "Test Agent 需要执行验证"
    接收:
      - "验证范围（contract/implementation/full）"
      - "项目路径"
      - "Tech Spec 位置"
    响应: "开始独立执行验证"

  # ========== 交付给 Test Agent ==========
  deliver_to_test_agent:
    交付物:
      - "验证报告（含所有证据链）"
      - "Skills 协作状态汇总"
      - "判定结果（PASS/CONDITIONAL_PASS/FAIL）"
      - "问题列表（如有）"
    格式: |
      # 验证执行官报告

      ## 执行摘要
      - 执行时间：{timestamp}
      - 验证范围：{scope}
      - 判定结果：{verdict}

      ## 独立执行验证结果
      | 验证项 | 结果 | 证据 |
      |--------|------|------|
      | VE-01 编译 | ✅/❌ | [链接] |
      | VE-02 Lint | ✅/❌ | [链接] |
      | ... | ... | ... |

      ## Skills 协作验证结果
      | 验证项 | 结果 | 证据 |
      |--------|------|------|
      | VS-01 Shared | ✅/❌ | [链接] |
      | VS-02 Backend | ✅/❌ | [链接] |
      | ... | ... | ... |

      ## 问题列表
      {issues}

      ## 完整证据链
      {evidence_chain}
```

---

## 十、版本历史

```yaml
changelog:
  v1.1:
    date: "2026-02-06"
    changes:
      - "新增便捷接口：execute_verification(type)"
      - "新增便捷接口：verify_skills_status(layer)"
      - "新增专用接口：verify_frontend_dependencies()"
      - "新增专用接口：verify_api_contract()"
      - "新增汇总接口：get_collaboration_summary()"
      - "修复与 Test Agent 接口调用的对齐问题"

  v1.0:
    date: "2026-02-02"
    changes:
      - "初版发布"
      - "定义独立执行清单（VE-01 ~ VE-06）"
      - "定义 Skills 协作状态验证（VS-01 ~ VS-05）"
      - "定义证据链生成规范"
      - "定义 10 条铁律（VX-01 ~ VX-10）"
```
