# 🏛️ Orchestra 架构规范（永乐大典）

> 版本：v3.0
> 更新：2026-01-25
> 适用范围：所有 Agent、Skill 和项目
> 状态：**强制执行**

---

## 📌 目录

1. [组件总览](#一组件总览)
2. [Agent-Skill 依赖关系](#二agent-skill-依赖关系)
3. [完整工作流程](#三完整工作流程)
4. [强制规则](#四强制规则)
5. [核心架构维度](#五核心架构维度)
6. [铁律汇总](#六铁律汇总)
7. [地基架构](#七地基架构) 🆕
8. [模块化架构](#八模块化架构) 🆕
9. [版本历史](#九版本历史)

---

## 一、组件总览

### 1.1 Agent 层（6个）

| Agent | 别名 | 职责 | 版本 |
|-------|------|------|------|
| **Conductor Agent** | 内阁首辅 | 总指挥、流程协调、问题路由、多项目管理 | v1.2 |
| **Plan Agent** | 翰林院学士 | 需求规划、项目采访 | v2.2 |
| **Spec Agent** | 工部尚书 | 技术规格、模块设计 | v1.7 |
| **Code Agent** | 工部侍郎 | 代码实现、Phase A/B | v1.7 |
| **Test Agent** | 工部主事 | 质量验收、契约验证、TDD、Eval | v2.0 |
| **Review Agent** | 都察院御史 | 代码审查、最终验收、项目说明、OWASP 安全审查 | v2.0 |

### 1.2 Core Skill 层（4个）

| Skill | 别名 | 职责 | 版本 |
|-------|------|------|------|
| **dialogue-archivist** | 史官 | 全程记录、存档追溯、持续学习 | v1.8 |
| **project-scanner** | 钦天监 | 项目扫描、真实禀报 | v1.2 |
| **module-planner** | 将作监 | 模块规划、命名规范 | v1.4 |
| **contract-guardian** | 契约守卫 | 契约验证、快照管理 | v1.4 |

### 1.3 Coder Skill 层（5个）

| Skill | 负责端 | 版本 |
|-------|--------|------|
| **shared-coder** | 共享层（types, utils, hooks） | v1.1 |
| **backend-coder** | 后端（NestJS, API） | v1.2 |
| **web-coder** | Web 前端（React） | v1.2 |
| **mobile-coder** | 移动端（React Native） | v1.2 |
| **desktop-coder** | 桌面端（Electron） | v1.2 |

### 1.4 Template/Validator Skill 层（3个）

| Skill | 职责 | 版本 |
|-------|------|------|
| **requirement-template** | 需求采集模板 | v1.0 |
| **spec-template** | 技术规格模板 | v1.1 |
| **tech-validator** | 技术方案校验 | v1.1 |

### 1.5 Infrastructure Skill 层（1个）

| Skill | 别名 | 职责 | 版本 |
|-------|------|------|------|
| **hooks-system** | 钩子系统 | 生命周期钩子、自动化触发、事件拦截 | v1.0 |

---

## 二、Agent-Skill 依赖关系

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    Orchestra / 永乐大典 组件依赖关系图                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌────────────┐   │
│  │Plan Agent │▶│Spec Agent │▶│Code Agent │▶│Test Agent │▶│Review Agent│   │
│  │ 翰林院学士 │ │ 工部尚书  │ │ 工部侍郎  │ │ 工部主事  │ │ 都察院御史 │   │
│  └─────┬─────┘ └─────┬─────┘ └─────┬─────┘ └─────┬─────┘ └──────┬─────┘   │
│        │             │             │             │              │          │
│        ▼             ▼             ▼             ▼              ▼          │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │                        Core Skills 层                                │  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────────────┐          │  │
│  │  │   史官   │ │  钦天监  │ │  将作监  │ │   契约守卫      │          │  │
│  │  │ 全程记录 │ │ 项目扫描 │ │ 模块规划 │ │ 契约验证       │          │  │
│  │  └──────────┘ └──────────┘ └──────────┘ └────────────────┘          │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
│                                    │                                       │
│                                    ▼                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │                        Coder Skills 层                               │  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │  │
│  │  │ shared   │ │ backend  │ │   web    │ │  mobile  │ │ desktop  │  │  │
│  │  │  coder   │ │  coder   │ │  coder   │ │  coder   │ │  coder   │  │  │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘  │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
│                                    │                                       │
│                                    ▼                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │                     Infrastructure 层 (基础设施)                      │  │
│  │  ┌─────────────────────────────────────────────────────────────┐    │  │
│  │  │                      hooks-system 钩子系统                    │    │  │
│  │  │  SessionStart → PreToolUse → PostToolUse → Stop → SessionEnd │    │  │
│  │  └─────────────────────────────────────────────────────────────┘    │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.1 各 Agent 调用的 Skill

```yaml
agent_skill_mapping:

  Plan_Agent:
    必须调用:
      - dialogue-archivist   # 记录采访过程
      - project-scanner      # 扫描已有项目
    可选调用:
      - requirement-template # 需求模板
      
  Spec_Agent:
    必须调用:
      - dialogue-archivist   # 记录设计决策
      - module-planner       # 模块规划
      - contract-guardian    # 契约格式验证
    可选调用:
      - project-scanner      # 扫描现有功能
      - spec-template        # 规格模板
      - tech-validator       # 技术方案校验
      
  Code_Agent:
    必须调用:
      - dialogue-archivist   # 记录开发过程
      - module-planner       # 检查命名和依赖
      - contract-guardian    # 契约验证
    可选调用:
      - project-scanner      # 扫描代码质量
      - shared-coder         # 共享层代码
      - backend-coder        # 后端代码
      - web-coder            # Web 前端代码
      - mobile-coder         # 移动端代码
      - desktop-coder        # 桌面端代码
      
  Test_Agent:
    必须调用:
      - contract-guardian    # 契约验收
      - dialogue-archivist   # 记录验收结果
    可选调用:
      - project-scanner      # 扫描代码质量
      - module-planner       # 检查命名规范
      
  Review_Agent:
    必须调用:
      - dialogue-archivist   # 记录审查过程
      - project-scanner      # 扫描代码质量
    可选调用:
      - module-planner       # 检查模块规范
      - contract-guardian    # 验证契约一致性
```

---

## 三、完整工作流程

### 3.1 项目生命周期

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         项目生命周期流程                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │ Phase 0: 需求规划 (Plan Agent)                                        │  │
│  │   • 项目采访（新项目/已有项目）                                       │  │
│  │   • 生成 Plan Report                                                  │  │
│  │   产出: plan_report.md                                                │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                    ↓                                        │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │ Phase 1: 技术规格 (Spec Agent)                                        │  │
│  │   • 模块规划（调用将作监）                                            │  │
│  │   • 生成 Tech Spec                                                    │  │
│  │   产出: tech_spec.md, modules.yaml                                    │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                    ↓                                        │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │ Phase 2A: 契约层开发 (Code Agent Phase A)                             │  │
│  │   • 创建类型定义、接口签名、API 路由                                  │  │
│  │   • 编译验证 → 契约快照 → 等待验收                                    │  │
│  │   产出: 契约代码（types, interfaces, API routes）                     │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                    ↓                                        │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │ Phase 2A-Test: 契约验收 (Test Agent)                                  │  │
│  │   • 调用契约守卫验证完整性                                            │  │
│  │   • 验证签名一致性                                                    │  │
│  │   • 验收通过 → 契约锁定 🔒                                            │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                    ↓                                        │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │ Phase 2B: 实现层开发 (Code Agent Phase B)                             │  │
│  │   • 填充函数体、实现业务逻辑                                          │  │
│  │   • 契约签名不可改（除非走变更流程）                                  │  │
│  │   产出: 完整代码                                                      │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                    ↓                                        │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │ Phase 3: 最终验收 (Test Agent)                                        │  │
│  │   • 契约违规检测                                                      │  │
│  │   • 编译、测试、代码质量                                              │  │
│  │   • 验收通过 → 交 Review Agent                                        │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                    ↓                                        │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │ Phase 4: 代码审查 (Review Agent) 🆕                                   │  │
│  │   • 深度代码审查（结构、逻辑、安全、质量）                            │  │
│  │   • 生成审查报告                                                      │  │
│  │   • 用户确认 → 交付                                                   │  │
│  │   产出: review_report.md                                              │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 三种场景适配

| 场景 | 流程 | Phase A/B |
|------|------|-----------|
| **新项目开发** | Plan → Spec → Code → Test | 完整 Phase A + B |
| **功能迭代** | (判断是否需要契约变更) → Code → Test | 轻量 Phase A（如有新契约）+ Phase B |
| **项目重塑** | Scan → Spec → 契约迁移 → 批次迁移 → Test | 契约迁移 + 分批次 Phase B |

---

## 四、强制规则

### 规则 1：扫描必须经由钦天监

```yaml
rule_1_scanner:
  name: "唯一扫描入口"
  description: "所有 Agent 了解项目现状，必须通过钦天监扫描"
  
  禁止行为:
    - "Agent 自行'扫描'或'查看'项目"
    - "凭记忆或推测描述项目现状"
    - "美化或隐瞒扫描结果"
    
  必须行为:
    - "调用钦天监接口获取真实数据"
    - "如实呈现扫描结果"
    - "保留证据来源（文件路径、行号）"
```

### 规则 2：记录必须经由史官

```yaml
rule_2_archivist:
  name: "唯一记录入口"
  description: "所有 Agent 的对话、决策、事件，必须通过史官记录"
  
  必须行为:
    - "项目启动调用 init_project"
    - "阶段开始调用 register_stage"
    - "重要决策调用 report_decision"
    - "阶段完成调用 complete_stage"
```

### 规则 3：用户确认才生效

```yaml
rule_3_user_confirm:
  name: "用户确认原则"
  description: "重要决策、产出、理解必须经用户确认"
  
  必须确认:
    - "需求理解（Plan Agent）"
    - "模块设计（Spec Agent）"
    - "契约锁定（Code Agent Phase A）"
    - "契约变更（任意阶段）"
```

### 规则 4：执行前必读铁律

```yaml
rule_4_read_laws:
  name: "必读铁律清单"
  description: "所有 Agent 执行前必读 CLAUDE.md"
  
  文件: "CLAUDE.md（< 50 行）"
  时机: "每次执行前"
```

---

## 五、核心架构维度

### 5.1 依赖层级（开发顺序）

```yaml
dependency_layers:
  原则: "底层先行，上层后建"
  规则: "每层只能依赖下层，不能依赖上层"
  
  shared:
    L0: [configs]
    L1: [types]
    L2: [utils]
    L3: [services]
    L4: [hooks]
    
  backend:
    L0: [configs]
    L1: [models]
    L2: [repositories]
    L3: [services]
    L4: [api]
    L5: [middlewares]
    
  ui:
    L0: [configs]
    L1: [components]
    L2: [pages/screens]
    L3: [navigation/router]
```

### 5.2 验收层级（测试顺序）

```yaml
verification_layers:
  原则: "逐层验证，下层失败则不进行上层"
  
  L1_编译验证: "tsc + eslint → 0 错误"
  L2_单元测试: "Jest/Vitest → 通过率 ≥ 80%"
  L3_集成测试: "API + 组件 → 核心流程 100%"
  L4_质量检查: "钦天监 + 将作监 → 无严重问题"
  L5_规格符合: "对比 Tech Spec → 100% 符合"
```

### 5.3 契约与实现

```yaml
contract_vs_implementation:

  契约层:
    标识: "🔒 锁定"
    内容: "interface, type, API 签名, 数据库结构"
    规则: "不可擅自修改，改必走变更流程"
    
  实现层:
    标识: "🔓 可优化"
    内容: "函数体, 算法, 性能优化, 错误处理"
    规则: "在契约不变前提下可自由优化"
```

---

## 六、铁律汇总

### 6.1 全局铁律 (CLAUDE.md)

| 编号 | 铁律 |
|------|------|
| CK-01 | grep 空输出 ≠ 不存在，必须 view 确认 |
| CK-02 | 报告"缺少"前必须展示 view 证据 |
| CK-03 | 流程：grep 定位 → view 确认 → 再报告 |
| V-01 | "已完成"必须附带真实输出 |
| V-02 | 禁止"应该"、"理论上"等模糊词 |

### 6.2 Agent 铁律

| Agent | 铁律编号 | 数量 |
|-------|----------|------|
| Code Agent | CA-01 ~ CA-21 | 21 |
| Test Agent | TA-01 ~ TA-17 | 17 |
| Spec Agent | SP-01 ~ SP-20 | 20 |
| Review Agent | RA-01 ~ RA-15 | 15 |
| Conductor Agent | CO-01 ~ CO-18 | 18 |
| Plan Agent | PA-01 ~ PA-13 | 13 |

### 6.3 Skill 铁律

| Skill | 铁律编号 | 数量 |
|-------|----------|------|
| 史官 | DA-01 ~ DA-12 | 12 |
| 钦天监 | PS-01 ~ PS-04 | 4 |
| 将作监 | MP-01 ~ MP-06 | 6 |
| 契约守卫 | CG-01 ~ CG-13 | 13 |
| 钩子系统 | HK-01 ~ HK-04 | 4 |

### 6.4 铁律分层与优先级 🆕 v2.9

```yaml
iron_law_hierarchy:

  description: |
    110+ 条铁律数量庞大，可能存在：
    1. 执行时遗漏
    2. 铁律之间冲突
    3. 优先级不明确

    解决方案：铁律分层

  # ══════════════════════════════════════════════════════════════════
  #  铁律金字塔
  # ══════════════════════════════════════════════════════════════════
  #
  #                    ┌─────────┐
  #                    │ 红线    │  ← 绝对不可违反（7条）
  #                    │ Level 0 │
  #                    └────┬────┘
  #                         │
  #                   ┌─────┴─────┐
  #                   │ 核心铁律  │  ← 强制执行（~30条）
  #                   │ Level 1   │
  #                   └─────┬─────┘
  #                         │
  #              ┌──────────┴──────────┐
  #              │ 标准规则            │  ← 应当遵守（~50条）
  #              │ Level 2             │
  #              └──────────┬──────────┘
  #                         │
  #         ┌───────────────┴───────────────┐
  #         │ 建议实践                      │  ← 推荐遵守（~30条）
  #         │ Level 3                       │
  #         └───────────────────────────────┘
  #
  # ══════════════════════════════════════════════════════════════════

  levels:

    level_0_red_line:
      name: "红线铁律"
      alias: "绝对禁区"
      description: "无论任何情况都不可违反"
      violation_consequence: "项目作废 / 立即停止 / 回滚"
      override_possible: false
      count: 7
      items:
        - code: "RL-01"
          source: "CA-11"
          rule: "禁止虚报：声称完成必须真完成"
          consequence: "虚报导致项目失败"

        - code: "RL-02"
          source: "DA-01"
          rule: "用户原始需求锁定不可改"
          consequence: "需求被篡改，项目方向错误"

        - code: "RL-03"
          source: "CA-17"
          rule: "契约锁定后不可擅自修改"
          consequence: "契约混乱，验收失败"

        - code: "RL-04"
          source: "R1"
          rule: "扫描必须经由钦天监"
          consequence: "扫描结果不可信"

        - code: "RL-05"
          source: "R2"
          rule: "记录必须经由史官"
          consequence: "历史不可追溯"

        - code: "RL-06"
          source: "R3"
          rule: "重要决策必须用户确认"
          consequence: "违背用户意图"

        - code: "RL-07"
          source: "CA-12"
          rule: "代码必执行 + 必须输出证据"
          consequence: "无法验证实际效果"

    level_1_core:
      name: "核心铁律"
      alias: "强制执行"
      description: "正常情况必须遵守，极端情况可请示皇上豁免"
      violation_consequence: "需要修复 / 重新执行"
      override_possible: true
      override_requires: "皇上明确批准"
      count: "~30"
      examples:
        - "CA-16: Phase A 必须等 Test Agent 验收"
        - "PA-03: 记录完整，调用史官存档"
        - "TA-05: 测试必须有断言"
        - "SP-19: 重大技术决策必须有 ADR"

    level_2_standard:
      name: "标准规则"
      alias: "应当遵守"
      description: "正常情况应当遵守，特殊情况可灵活处理"
      violation_consequence: "记录偏离原因"
      override_possible: true
      override_requires: "Agent 判断 + 记录原因"
      count: "~50"
      examples:
        - "命名规范相关铁律"
        - "代码质量相关铁律"
        - "文档格式相关铁律"

    level_3_recommendation:
      name: "建议实践"
      alias: "推荐遵守"
      description: "最佳实践，不强制但推荐"
      violation_consequence: "无强制后果"
      override_possible: true
      override_requires: "无需审批"
      count: "~30"
      examples:
        - "性能优化建议"
        - "可选的代码风格"
        - "非核心的注释要求"

  # 冲突解决规则
  conflict_resolution:

    principle: "高层级优先"

    rules:
      - name: "红线优先"
        rule: "红线铁律与任何其他铁律冲突时，红线优先"
        example: "即使效率低，也要保证证据完整（RL-07）"

      - name: "核心优先于标准"
        rule: "核心铁律与标准规则冲突时，核心优先"
        example: "即使代码不够优雅，也要保证功能正确"

      - name: "用户意图最优先"
        rule: "当铁律与用户明确要求冲突时，请示用户"
        example: "用户要求跳过测试，需明确告知风险后由用户决定"

    conflict_scenarios:
      - scenario: "快速交付 vs 充分测试"
        resolution: "充分测试（核心）优先，但可协商测试范围"

      - scenario: "代码优雅 vs 快速实现"
        resolution: "快速实现优先（标准），但记录技术债务"

      - scenario: "严格规范 vs 用户习惯"
        resolution: "请示用户，用户习惯优先时记录偏离

  # 铁律执行检查清单
  execution_checklist:

    agent_startup:
      必检红线:
        - "RL-01: 不虚报"
        - "RL-04: 扫描走钦天监"
        - "RL-05: 记录走史官"
      建议检查:
        - "当前阶段的核心铁律"

    agent_completion:
      必检红线:
        - "RL-07: 有执行证据"
        - "RL-03: 契约未被破坏"
      必检核心:
        - "该阶段的核心铁律"

  # 铁律遵守报告（可选）
  compliance_report:
    description: "可选功能：生成铁律遵守报告供用户查看"
    sections:
      - "红线铁律：全部通过 ✅"
      - "核心铁律：28/30 通过（2条已获豁免）"
      - "标准规则：45/50 通过（5条记录偏离）"
      - "建议实践：20/30 遵守"
```

---

## 七、地基架构 🆕 v3.0

### 7.1 地基定位

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│  永乐大典 = AI 驱动的标准化开发体系                                         │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                     Orchestra 工作流层                               │   │
│  │    Plan → Spec → Code → Test → Review                               │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                     业务模块层                                       │   │
│  │    Auth │ Storage │ Notify │ Payment │ ...                          │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                     地基模块层                                       │   │
│  │    Logger │ ErrorKit │ Config │ TestKit                             │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                     统一地基规范                                     │   │
│  │    Layer 0~4: 身份 │ 可观测性 │ 错误处理 │ 配置 │ 质量              │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 7.2 地基五层结构

| 层级 | 名称 | 目的 | 详见 |
|------|------|------|------|
| Layer 0 | 项目身份 | 每个项目的"身份证" | FOUNDATION.md |
| Layer 1 | 可观测性 | 出问题时能快速定位 | FOUNDATION.md |
| Layer 2 | 错误处理 | 统一的错误分类和处理 | FOUNDATION.md |
| Layer 3 | 配置管理 | 环境变量、敏感信息 | FOUNDATION.md |
| Layer 4 | 质量规范 | 测试、文档、代码风格 | FOUNDATION.md |

### 7.3 地基强制规则

```yaml
规则_F1:
  name: "项目必须有身份证"
  rule: "所有项目必须包含 .orchestra/project.yaml"

规则_F2:
  name: "必须使用地基模块"
  rule: "所有项目必须使用 Logger、ErrorKit、Config 地基模块"

规则_F3:
  name: "日志格式统一"
  rule: "所有日志必须遵循 [时间][级别][模块][请求ID] 消息 格式"

规则_F4:
  name: "错误码统一"
  rule: "所有错误必须使用 {模块码}{类型码}{序号} 格式"

规则_F5:
  name: "配置方式统一"
  rule: "环境变量必须使用 {项目前缀}_{模块}_{配置项} 格式"
```

### 7.4 地基检查清单

```yaml
项目合规检查:
  Layer_0:
    - ".orchestra/ 目录存在"
    - "project.yaml 格式正确"
    - "所有必填字段已填写"

  Layer_1:
    - "使用 @orchestra/logger"
    - "日志格式符合规范"
    - "Request ID 机制已实现"

  Layer_2:
    - "使用 @orchestra/error-kit"
    - "错误码遵循规范"
    - "错误响应格式统一"

  Layer_3:
    - ".env.example 存在"
    - "敏感信息未提交到 git"
    - "环境变量命名规范"

  Layer_4:
    - "README.md 包含必要章节"
    - "测试覆盖率 ≥ 60%"
    - "Linter 配置存在"
```

---

## 八、模块化架构 🆕 v3.0

### 8.1 模块分类

| 类别 | 名称 | 说明 | 强制性 |
|------|------|------|--------|
| 地基模块 | Foundation Modules | 实现地基规范的基础模块 | 必须使用 |
| 业务模块 | Business Modules | 可复用的业务功能模块 | 按需使用 |

### 8.2 地基模块清单

| 模块 | 用途 | TypeScript | Python |
|------|------|------------|--------|
| Logger | 统一日志 | @orchestra/logger-ts | orchestra-logger |
| ErrorKit | 错误处理 | @orchestra/error-kit-ts | orchestra-error-kit |
| Config | 配置管理 | @orchestra/config-ts | orchestra-config |
| TestKit | 测试工具 | @orchestra/test-kit-ts | orchestra-test-kit |

### 8.3 业务模块清单（示例）

| 模块 | 用途 | 实现方式 |
|------|------|----------|
| Auth | 用户认证 | 服务/库 |
| Storage | 文件存储 | 服务/库 |
| Notify | 消息通知 | 服务 |
| Payment | 支付处理 | 服务 |

### 8.4 模块仓库组织

```
orchestra/
├── packages/
│   ├── foundation/              # 地基模块
│   │   ├── logger-ts/
│   │   ├── logger-py/
│   │   ├── error-kit-ts/
│   │   └── ...
│   └── modules/                 # 业务模块
│       ├── auth/
│       ├── storage/
│       └── ...
├── services/                    # 独立服务
│   ├── auth-service/
│   └── notify-service/
├── templates/                   # 项目模板
│   ├── web-fullstack/
│   ├── cli/
│   └── library/
└── docs/
    ├── FOUNDATION.md
    └── MODULES.md
```

### 8.5 模块版本管理

```yaml
版本号规范: "MAJOR.MINOR.PATCH (SemVer)"

地基模块:
  策略: "锁定版本同步"
  说明: "所有地基模块使用相同的主版本号"
  示例: "logger@1.2.0, error-kit@1.2.0, config@1.2.0"

业务模块:
  策略: "独立版本"
  说明: "各业务模块独立迭代"
  示例: "auth@2.1.0, storage@1.5.0"
```

### 8.6 详细规范

完整的地基规范和模块化架构详见：
- **FOUNDATION.md** - 地基五层详细规范
- **MODULES.md** - 模块化架构详细规范

---

## 九、版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v3.0 | 2026-01-25 | **重大更新**：新增地基架构（FOUNDATION.md）和模块化架构（MODULES.md），定义统一地基五层规范、地基模块、业务模块、项目模板体系 |
| v2.9 | 2026-01-25 | 新增铁律分层机制：红线铁律（7条）、核心铁律、标准规则、建议实践四层，冲突解决规则，执行检查清单 |
| v2.8 | 2026-01-25 | 融合完成：Plan Agent v2.3（PA-13）、Spec Agent v1.9（SP-16~20）、Code Agent v1.8（CA-21），铁律总数更新为 110 条 |
| v2.7 | 2026-01-25 | Review Agent v2.0：融合 Security Reviewer + Code Reviewer，新增 D6 性能维度、OWASP Top 10 完整覆盖、安全工具规范、紧急响应流程，铁律增至 15 条（RA-13~15） |
| v2.6 | 2026-01-25 | Test Agent v2.0：融合 Verification Loop + TDD + Eval Harness，铁律增至 17 条，铁律总数更新为 98 条 |
| v2.5 | 2026-01-24 | 新增 hooks-system 基础设施层、史官铁律增至 12 条、铁律总数更新为 95 条 |
| v2.4 | 2026-01-24 | Plan Agent 铁律（PA-01~PA-10）、铁律总数更新为 85 条 |
| v2.3 | 2026-01-24 | Conductor Agent v1.2: 多项目管理、铁律增至 18 条 |
| v2.2 | 2026-01-23 | 新增 Conductor Agent、更新 Agent 数量为 6、更新铁律汇总 |
| v2.1 | 2026-01-23 | 新增 Review Agent、更新流程图、更新铁律汇总 |
| v2.0 | 2026-01-23 | 重构：完整组件总览、依赖关系图、工作流程图、铁律汇总 |
| v1.2 | 2026-01-23 | 新增核心架构维度 |
| v1.1 | 2024-01-20 | 新增必读铁律清单 |
| v1.0 | 2024-01-20 | 初始版本 |

---

**🏛️ Orchestra / 永乐大典 架构规范 v3.0 · 完**
